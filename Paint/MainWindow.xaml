<Window x:Class="Paint.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Paint"
        mc:Ignorable="d"
        Title="Paint" Height="455" Width="800" ResizeMode="NoResize" Loaded="Window_Loaded">
    <DockPanel>
        <Border Panel.ZIndex="-1" Padding="5" Background="DarkCyan" BorderBrush="#001427" BorderThickness="3">
            <StackPanel  Width="200">
                <StackPanel.Resources>
                    <Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="#001427"></Setter>
                        <Setter Property="FontSize" Value="22"></Setter>
                        <Setter Property="Margin" Value="0 0 6 3"></Setter>
                        <Setter Property="FontWeight" Value="DemiBold"></Setter>
                    </Style>
                </StackPanel.Resources>
                <TextBlock>Figura:</TextBlock>
                <ComboBox SelectionChanged="cmbFigura_SelectionChanged" FontSize="20" HorizontalContentAlignment="Center" Name="cmbFigura" SelectedIndex="0">
                    <ComboBoxItem>Linea</ComboBoxItem>
                    <ComboBoxItem>Rectangulo</ComboBoxItem>
                    <ComboBoxItem>Curva</ComboBoxItem>
                    <ComboBoxItem>Ellipse</ComboBoxItem>
                    <ComboBoxItem>Borrador</ComboBoxItem>
                </ComboBox>
                <TextBlock>Grosor</TextBlock>
                <Slider ValueChanged="sldGrosor_ValueChanged" Value="1" Name="sldGrosor" Minimum="0" Maximum="10"></Slider>
                <TextBlock>Color(RGB)</TextBlock>
                <DockPanel>
                    <TextBlock>R</TextBlock>
                    <Slider ValueChanged="Red_ValueChanged" Value="0" Minimum="0" Maximum="255" Name="Red" VerticalAlignment="Center"></Slider>
                </DockPanel>
                <DockPanel>
                    <TextBlock>G</TextBlock>
                    <Slider ValueChanged="Red_ValueChanged" Value="0" Minimum="0" Maximum="255" Name="Green" VerticalAlignment="Center"></Slider>
                </DockPanel>
                <DockPanel>
                    <TextBlock>B</TextBlock>
                    <Slider ValueChanged="Red_ValueChanged" Value="0" Minimum="0" Maximum="255" Name="Blue" VerticalAlignment="Center"></Slider>
                </DockPanel>
                <Ellipse Width="100" Stroke="Black" StrokeThickness="2" Height="100" Name="rectanguloColor"></Ellipse>
                <Button Margin="0 5 0 0" Click="Button_Click" Background="Red" Foreground="White" FontSize="15"  Content="Borrar"></Button>
                <Button Content="Guardar" Click="Button_Click_1" Background="Green" Foreground="White" FontSize="15" Margin="0 5 0 0"></Button>
            </StackPanel>
        </Border>
        <Border BorderBrush="#001427" BorderThickness="3">
            <Canvas MouseLeave="Lienzo_MouseLeave" MouseUp="Lienzo_MouseUp" MouseDown="Lienzo_MouseDown" MouseMove="Lienzo_MouseMove" Background="White" Name="Lienzo"></Canvas>
        </Border>
    </DockPanel>
</Window>
